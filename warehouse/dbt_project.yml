name: "metrics_warehouse"
version: "1.0.0"
config-version: 2

profile: "metrics_warehouse"

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  metrics_warehouse:
    staging:
      +materialized: view
    marts:
      +materialized: table
